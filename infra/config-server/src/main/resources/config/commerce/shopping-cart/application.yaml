application:
  name: shopping-cart

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/smarthome?currentSchema=cart
    username: "dbuser"
    password: "12345"
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        default_schema: cart
        format_sql: true
  sql:
    init:
      mode: always
server:
  port: 0