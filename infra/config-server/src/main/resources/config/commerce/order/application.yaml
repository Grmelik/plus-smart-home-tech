spring:
  application:
    name: order
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/smarthome?currentSchema=orders
    username: "dbuser"
    password: "12345"
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        default_schema: orders
        format_sql: true
  sql:
    init:
      mode: always
server:
  port: 0