application:
  name: delivery

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/smarthome?currentSchema=delivery
    username: "dbuser"
    password: "12345"
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        default_schema: delivery
        format_sql: true
  sql:
    init:
      mode: always
server:
  port: 0